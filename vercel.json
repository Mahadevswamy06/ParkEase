{
  "buildCommand": "cd frontend/parkease-react && npm install && npm run build",
  "outputDirectory": "frontend/parkease-react/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
